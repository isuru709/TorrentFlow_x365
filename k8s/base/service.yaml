apiVersion: v1
kind: Service
metadata:
  name: torrent-downloader
  labels:
    app: torrent-downloader
    app.kubernetes.io/name: torrent-downloader
    app.kubernetes.io/part-of: torrentflow
spec:
  type: LoadBalancer
  selector:
    app: torrent-downloader
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
